apiVersion: v1
kind: Service
metadata:
  name: devconnect-app
  labels:
    app: devconnect-app
    tier: backend
spec:
  selector:
    app: devconnect-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP